import {
  getDeviceId,
  getSystemVersion,
  getSystemName,
  getPhoneNumber,
  getBuildId,
  getModel,
  getCarrier,
} from 'react-native-device-info';
import NetInfo from '@react-native-community/netinfo';

export {
  getDeviceId,
  getSystemVersion,
  getSystemName,
  getPhoneNumber,
  getBuildId,
  getModel,
  getCarrier,
  NetInfo,
};
